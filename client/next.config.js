/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com','images.bloomingdalesassets.com','firebasestorage.googleapis.com'],
    },
}

module.exports = nextConfig
